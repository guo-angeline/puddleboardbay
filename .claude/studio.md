# Studio config: paddlePuddleboardBay (sup-spots / paddletowater.com)

backlog: ROADMAP.md
layout: TWO PACKAGES since 2026-07-19: the Next.js site lives in web/ (web/app, web/components, web/lib, web/data, web/public), the Expo iOS app in native/. Root package.json only proxies scripts into web/ (`npm test` at root still works). Repo-root-relative paths in older notes (data/spots.json, lib/..., app/...) now mean web/data/..., web/lib/..., web/app/....
test: npm test (root proxy for `npm --prefix web test`; native tests: npm run test:native)
lint: npm run lint
build: npm run build (must pass before every deploy)
deploy: `vercel deploy --prod --cwd web` (the bare `vercel --prod --yes` form is REJECTED by the current CLI, which prints the `vercel deploy --prod` form back at you. Item 106 (2026-07-22) fixed predeploy-gate.py to trigger on BOTH forms, `is_prod_deploy` matches any command carrying `vercel` + `--prod`, so the working command no longer skips the gate; before that fix four deploys went out ungated. The manual pre-deploy checks are no longer strictly required, but the gate is a backstop, not a substitute for reading your own diff. CLI is the ONLY path to production; there is no git integration, a commit without a deploy changes nothing live). The Vercel project link lives in web/.vercel. After a verified deploy, move the pointer: git tag -f deployed-prod HEAD. The predeploy-gate.py hook diffs deployed-prod against the tree and stops a deploy that touches gated paths (a CHANGE to an existing spot's lat/lng per D19/D23; push/cron/subscription code, which since item 106 includes the send-path LIBRARIES `web/lib/alerts/*`, `web/lib/email/*` and `web/lib/launchDirection.ts`, not just the `web/app/api/*` routes; or any path on a Gates: line of an OPEN decision). Run `python3 scripts/predeploy-gate.py --selftest` if you touch the gate. When the OWNER explicitly approves a push-surface deploy in chat, write the HEAD sha to a repo-root DEPLOY_APPROVAL file (gitignored): it releases ONLY the PROTECTED reason, only for that exact sha, and must be deleted right after the deploy. Never write it without a live owner approval.
previewDeploy: vercel --cwd web (non-prod preview URL for verifier checks)
liveUrl: https://paddletowater.com
protectedBranches: [main]
autonomy: standard

notes: |
  - Read CLAUDE.md before any work; it is dense with load-bearing constraints.
  - PROTECTED (always escalate): changes to push-notification/cron send behavior (real users get woken up; the 6am-alert incident is why the cron moved to 7pm), Supabase subscription rows, destructive migrations.
  - Never alter lat/lng in data/spots.json unless explicitly correcting coordinates with a verified source; verify unchanged before deploy.
  - Every material UX change ships with an analytics event (trackIntent/trackSystem discipline per CLAUDE.md) and an analytics/INSTRUMENTATION_CHANGELOG.md entry.
  - BOARD DIRECTIVE (2026-07-17, SUPERSEDES the 2026-07-02 A/B rule this file used to carry): no A/B tests until DAU > 100. Ship major user-facing changes at 100% behind a `useKillSwitch` flag (default ON) for reversibility, and watch guardrails instead of reading a lift. Full reasoning in CLAUDE.md. Small fixes and copy tweaks are flag-exempt.
  - This project has its OWN palette (indigo accent #3730A3, Libre Baskerville + Nunito). Do not apply the Moss & Stone house palette here.
  - Deploy timing pollutes analytics windows: deploy promptly after verified merges, and note the deploy time in the briefing.
  - Spot notes stay general and evergreen, never phrased as replies to a specific person.
  - PostHog project 458289; analytics reports live in reports/; read analytics/INSTRUMENTATION_CHANGELOG.md before citing any metric.
  - OWNER DIRECTIVE (2026-07-02): ROADMAP.md is the ONLY vision/strategy/roadmap doc. Never create a new plan/roadmap/strategy file; fold everything into ROADMAP.md. docs/superpowers/ specs are historical execution artifacts for shipped work only.
  - Building in a worktree needs a REAL node_modules: Turbopack rejects a symlink that points outside the project root (npm run build dies with "Symlink node_modules is invalid"). Cross-device (/tmp worktree) so hardlinks fail too: `cp -a <root>/web/node_modules <worktree>/web/node_modules` (~12s, 563M) before `npm run build`. Tests + lint work fine over the symlink; only the build needs the copy. CONSEQUENCE (item 71, 2026-07-20): the ship workflow's per-task AND whole-branch verifiers run only tests + lint in the worktree, NEVER tsc/build, so a type error that tests+lint don't catch passes every workflow gate as "verify: pass" and then FAILS the Vercel deploy build. Next build typechecks the whole build graph (test-file tsc errors are pre-existing on main and don't gate; a real component error does). So in the main-session step-5 pass, before deploying, run `npx tsc --noEmit` (fast, over the symlink) and, for a real prod-parity check, `cp -a` node_modules + `npm run build`; do not trust a workflow "whole-branch verify pass" as proof the build is green. WORKTREE PATH TRAP (item 44, 2026-07-21): when an item touches REPO-ROOT-level paths (`supabase/migrations/`, `analytics/`, `docs/`, ROADMAP/DECISIONS) rather than `web/`, it is easy to write the new file to the ROOT repo by muscle memory instead of `<worktree>/...`, which dirties the primary tree you must not touch. Always write to the worktree path; after creating any root-level file, `git -C <root> status` should show ONLY your own ROADMAP lock edit, nothing else.
  - The D19/D23 coordinate gate fires ONLY on a CHANGE to an existing spot's lat/lng (a removed `-` lat/lng line vs deployed-prod), the "pin moves under live users and the crons" risk. Adding a NEW spot does not gate and needs no owner deploy approval (D23, owner directive 2026-07-17): ship it like any reversible content change. If you ever DO move an existing coordinate, that still gates: merge to main so the diff is the review artifact, escalate the DEPLOY, and note that an un-reviewed coordinate change left on main freezes later deploys too (it diffs the whole tree), so clear it promptly or sequence such items last. Never bypass the gate when it fires.
  - The sandboxed dev server has NO outbound network: a server-side fetch to an external API (a new /api route, a proxy) fails with HTTP 000 locally, so you CANNOT exercise the real upstream round-trip in local verification. Verify such routes three ways instead: (1) mocked-fetch unit tests for the route logic (retry/cache/error/passthrough); (2) the NOAA-independent paths live after deploy (e.g. a 400 on bad params proves the handler is wired); (3) the real upstream round-trip only post-deploy on Vercel, which has network, and even then the upstream itself may be down (NOAA was 5xx across all stations on 2026-07-17). Do not read a local HTTP 000 or a prod 502-during-upstream-outage as a bug in your route. New vitest test files under app/ need `app/**/*.test.ts` in vitest.config.ts include (added item 52). Separately, the Browser pane's console keeps a STALE error buffer: `read_console_messages` re-serves an old HMR compile error (e.g. a since-fixed "useKillSwitch defined multiple times" in AccountButton.tsx) long after the file is clean. It misled a verify pass twice (items 89, 97). Before treating a console error as real, confirm it against the CURRENT file (`grep -c` the symbol) and against `npm run build`; a green build means the error is buffer residue, not your change.
  - Uncommitted edits to ROADMAP.md are the owner's INDEPENDENT VERIFIER AGENT steering the backlog, not a human mid-keystroke and not a collision (owner confirmed 2026-07-17). That agent runs an audit + test pass every ~2h and writes its findings into item specs (e.g. the item 53 verify-loop measurement) for this loop to pick up and implement. So do NOT hold or park on an uncommitted ROADMAP edit: treat it as settled steering. Preserve it as its own labeled standalone commit (`git commit ROADMAP.md -m "Verifier steering: ..."`) to clean the tree, then work the item. **It also files NEW numbered items while you work, so item numbers collide:** pick a number immediately before writing, and re-check `grep -oE '^## [0-9]+\.' ROADMAP.md | ... | uniq -d` immediately AFTER, not before. Hit twice on 2026-07-22. Never stash/revert it, never absorb it into an unrelated commit, never edit its lines. (Only genuinely hold for uncommitted changes to CODE files or to an item OTHER than the one you're shipping, which would signal concurrent human/agent work in the tree.)
  - Lint gate `react-hooks/refs` is an ERROR here, hit twice (items 55, 53): never READ or WRITE `someRef.current` during render. Update a ref inside a useEffect, and for a value you need AT render time, use useState or carry it in the object you setState (e.g. put `fetchedAt` in the resolved slot, not a ref read in JSX). `npm run lint` catches it, but expect it and write it right the first time.
  - COORDINATES COME FROM IMAGERY, NOT FROM DATASETS. Every source in this project publishes a SITE LOCATOR (park centroid, street address, polygon centre): CCC's own launch-typed records reverse-geocoded to a parking lot, a road or a private house 20 times out of 20, and OSM has zero boat rentals and zero named beaches in Mission Bay, the densest paddle water in the state. A put-in is a physical feature, so LOOK AT IT: `https://basemap.nationalmap.gov/arcgis/rest/services/USGSImageryOnly/MapServer/export?bbox=<w,s,e,n>&bboxSR=4326&imageSR=4326&size=900,900&format=png&f=image`, then read the ramp, the sand, the staged craft. Public domain, no contract restrictions. **Google Places is disqualified for this app specifically**: its terms bar using Places content with a non-Google map and cap lat/lng caching at 30 days, and this app is Leaflet + CARTO with permanent coordinates. Reach for imagery BEFORE asking the owner to look a coordinate up; four of five San Diego pins were readable in about a minute each after the owner had already been handed a lookup list for them.
  - A LAWYER GATE'S REQUIRED ACTIONS ARE STANDING CONSTRAINTS, NOT ONE-TIME CHORES. Hit twice on 2026-07-21/22: the blended-score gate (D30) required a visible "this is our number" label AND an inputs breakdown in the sheet; two later owner-directed items removed each of them separately, and neither removal would have triggered a gate on its own reading ("it's a copy tweak"). The test that matters is not "does this change touch a legal surface" but "does this change UNDO something a prior gate required". Before shipping any removal on a surface a gate has already ruled on, grep DECISIONS.md for that gate's required actions and re-gate if the change reverses one. Cheap: the re-gate is one agent call. The failure mode is silent, because each removal looks like taste. TWO halves keep it from being silent, and item 85 proved the first half was missing: (a) GUARD GATE-REQUIRED COPY THE MOMENT IT SHIPS. Item 83's reader-facing disclosure was a required action that shipped with no test asserting it, so deleting it later broke nothing; a gate action that lives only in a JSX string is one edit from gone. (b) When a guard does encode a gate finding, rewrite it into a ledger of what still carries the requirement rather than deleting the assertion, so the NEXT removal has to read that list. A guard written the lazy way (asserting the old string is ABSENT) points the wrong way and will certify exactly the state the gate rejected: assert what must be PRESENT. (c) A REQUIRED ELEMENT CAN BE PRESENT, GUARDED, AND STILL ABSENT WHERE IT NOW MATTERS. Item 89: the reader's only Contributor Terms link was rendered `{hasReviews && ...}`, and the guard asserted it on that same branch, so both were green while the item added a SECOND branch (the zero-review invitation, ~176 of 177 spots) where the link would not have rendered at all. When an item adds a new branch to a surface a gate has ruled on, re-ask the gate's condition against the NEW branch, and widen the guard to cover it; checking that the element still exists somewhere is not the same question.
  - EXERCISE A GUARD, NEVER REASON ABOUT IT. Item 91: two agents disagreed over whether folding a push-behaviour fix into a UI bundle would trip the predeploy gate, and BOTH were wrong, because `PROTECTED_PATTERNS` in `scripts/predeploy-gate.py` matches on PATH (`web/app/api/cron/*`, `web/app/api/alerts/*`) and the library that actually decides which alerts fire, `web/lib/alerts/conditions-window.ts`, is not on it. Ten lines of python running the gate's own matcher over a fixture list settled it. The same move applies to any guard, hook or test you are about to rely on or argue from: run it against the case in question, or break the invariant on a scratch copy and confirm it goes red (both new guards this week were verified that way). A guard you have only read is a guard you are assuming.
  - Two pre-deploy checks the workflow gates do NOT make. (1) KILL SWITCH: the standing directive ships every major user-facing surface at 100% behind `useKillSwitch(...)`, default ON. An implementer will happily gate a feature on a *config* check (`authEnabled`, "is Supabase set up") and call that flag-gated; it is not, it cannot be flipped without a redeploy. Item 43 (2026-07-21) reached production that way and needed a follow-up deploy. Before deploying a new surface, grep the diff for `useKillSwitch` and confirm the switch also hides every derived artifact (an average whose source list is hidden is worse than no average). (2) VERIFY THE SERVER PATH YOURSELF, do not punt it to the owner. An auth-gated write path still has a downstream half you can exercise without a session: seed the row directly via the service role key from `web/.env.local`, drive the real public/moderation endpoints against production, assert each transition, then delete the seeded row in a `finally` block. Item 43's whole publish path (pending invisible -> approve link publishes -> replayed link refused -> aggregate withheld) was proven this way in one script. (3) A NEW USER-FACING CONTROL IS PLACED AGAINST THE EXISTING ONES, never invented standalone: before adding a button, list the controls already on that surface and decide whether the new one is a peer of one of them. Item 43 shipped "Write a review" as a lone bordered button under the review list while Share / Get directions / Photos sat in an action row below it, and the owner rejected it on sight ("designer is not doing its job"); it is now "Review", half-width beside Share, same treatment. If an item touches a surface that already has an action row, run design-lead or do this inventory by hand.
  - Local dev hides the accounts + reviews UI unless `web/.env.development.local` exists: the two `NEXT_PUBLIC_SUPABASE_*` keys live only in Vercel, never in `web/.env.local`, so `authEnabled()` is false locally and every account/review surface renders `null`. Recreate with `vercel env pull <scratch> --environment=production --cwd web` and copy just those two lines across (`.env*` is gitignored; the anon key is public by design). Without it you will "verify" a review change against a page that never rendered it. Vercel PREVIEW deploys are not the fallback: they sit behind Vercel login, so the browser tools get an auth page.
  - Diagnosing "the email never arrived": `vercel logs <deployment-url>` returns a timestamped request log (local time, `◇` static / `λ` function) and is the fastest way to correlate a backend timestamp with what actually hit the site. It cracked item 74: Supabase confirmed an address at 19:55:28.058Z and the log showed `GET /` at 19:55:28.25Z, proving a link scanner, not the user, had consumed the token. Two dead ends to skip: `RESEND_API_KEY` in `web/.env.local` is send-only (`restricted_api_key`), so delivery logs need the Resend dashboard, and Supabase email templates are not readable from any key we hold. Check the recipient's MX before advising a workaround (`nslookup -type=mx <domain>`): Cornell is Microsoft, so "just use Google sign-in" would have been wrong.
  - Dry backlog under the fixed 30-min cron (owner wants the loop alive; a verifier agent refills [ready] work every ~2h): the skill's "STOP the loop" assumes a ScheduleWakeup loop with a wakeup to skip. Here there is none, so do NOT CronDelete. When no [ready] item exists and >=1 [proposed] items already sit unpromoted, do NOT run product-visionary to add more (you cannot self-promote, so it just piles noise on the owner) and do NOT re-brief or re-push each dry fire (that nags). Just idle: one concise dry-state note the FIRST time it goes dry, then quiet fires until the verifier or an owner promotion/decision refills it. Only send the ONE reminder + consider stopping if a decision has gated everything for >24h (D24 opened 2026-07-17).
  - HARD RULE (owner directive 2026-07-18, "deleting a roadmap item is very muchly not okay"): NEVER delete a roadmap item. A `[done]` item is CONDENSED into a Shipped one-liner (its full block collapses, the item is not removed); every still-open item (`[proposed]`/`[ready]`/`[in-progress]`/`[blocked]`/`[parked]`) stays in the file, full block intact, until the OWNER removes it. Do not "clean up", "consolidate", or drop items you judge stale, superseded, or duplicate, flag them to the owner instead. Before committing ANY ROADMAP edit, diff the item-heading set (`grep -oE '^## [0-9]+\. ' old vs new`); if any heading disappeared that you did not move to Shipped, you deleted an item, undo it. This already cost us four items (31 spot-photos, 35 ToS/assumption-of-risk waiver [LEGAL], 45 coverage, 46 launch-reminder safety line) in commit 7063d16, restored 2026-07-18.
