{
  "name": "paddle-to-water",
  "private": true,
  "description": "Monorepo root. The Next.js site lives in web/, the Expo iOS app in native/. Each keeps its own package.json, lockfile, and node_modules (intentionally NOT npm workspaces: hoisting would let Metro resolve web's React copy).",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web test",
    "test:native": "npm --prefix native test",
    "ios": "npm --prefix native run ios"
  }
}
